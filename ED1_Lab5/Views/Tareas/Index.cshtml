@model IEnumerable<ED1_Lab5.Models.TareaPendiente>

@{
    ViewBag.Title = "Index";
}

<h2>Tareas Developer</h2>
@Html.ActionLink("Ver siguiente tarea a Realizar", "PrioridadTareas", new { id = 0 })
<p>

</p>

<p>
    @Html.ActionLink("Agregar Nueva Tarea", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Prioridad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proyecto)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.FechaEntrega)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Prioridad)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proyecto)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.FechaEntrega)
            </td>
            <td>
                @Html.ActionLink("Tarea Finalizada", "PrioridadTareas", new { id = 0 })
            </td>
        </tr>
    }

    @Html.ActionLink("Cerrar Sesión", "PaginaPrincipal")

</table>
